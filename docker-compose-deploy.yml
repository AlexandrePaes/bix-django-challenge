version: '3.9'

services:
  gcloud:
    image: google/cloud-sdk:latest
    volumes:
      - gcp-creds:/creds
      - .:/appbix
    working_dir: /appbix
    environment:
      - CLOUDSDK_CONFIG=/creds

volumes:
  gcp-creds: